import os
import sys
# append current directory to the search path (so import easybuild... doesn't need to change to import lib.easybuild...)
sys.path.append(os.path.abspath(os.path.dirname(__file__)))
