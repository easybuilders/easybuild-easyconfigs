easyblock = "Tarball"

name = 'Neo4j'
version = '4.2.5'
versionsuffix = '-CE'

homepage = 'https://neo4j.com'
description = """Neo4j (community edition) is a native graph database, built from the ground up to leverage not only
data but also data relationships. Neo4j connects data as it’s stored, enabling queries never before imagined, at
speeds never thought possible."""

toolchain = SYSTEM

source_urls=['https://neo4j.com/artifact.php?name=']
sources = ['neo4j-community-%(version)s-unix.tar.gz']
checksums = ['6d9bb8e21a4b80f8edefff89d6fe592812ae26eaf105cbc967bce88b62290bfd']

dependencies = [('Java', '11')]

sanity_check_paths = {
    'files': ['bin/neo4j'],
    'dirs': []
}

sanity_check_commands = ['neo4j version']

moduleclass = 'cae'
