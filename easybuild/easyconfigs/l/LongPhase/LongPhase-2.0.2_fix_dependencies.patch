diff -ruN longphase-2.0.2/Makefile longphase-2.0.2-patched/Makefile
--- longphase-2.0.2/Makefile	2026-05-04 09:17:11.000000000 +0200
+++ longphase-2.0.2-patched/Makefile	2026-08-24 11:32:46.129825591 +0200
@@ -2,10 +2,10 @@
 CXX      = g++
 AR       = ar
 AWK      = awk
-CFLAGS   = -g -Wall -O2 -pedantic -std=c99 -D_XOPEN_SOURCE=600
-CPPFLAGS = -std=c++11 -g -Wall -O3 -fopenmp
-LDFLAGS  =
-LIBS     =
+#CFLAGS   = -g -Wall -O2 -pedantic -std=c99 -D_XOPEN_SOURCE=600
+#CPPFLAGS = -std=c++11 -g -Wall -O3 -fopenmp
+#LDFLAGS  =
+#LIBS     =
 
 OBJ = Haplotag.o ParsingBam.o Util.o HaplotagProcess.o PhasingProcess.o Phasing.o PhasingGraph.o ModCall.o ModCallParsingBam.o ModCallProcess.o main.o
 
@@ -14,12 +14,12 @@
 all: subprojects
 
 subprojects:
-	$(MAKE)	-C	./jemalloc
-	$(MAKE)	-C	./htslib
+	#$(MAKE)	-C	./jemalloc
+	#$(MAKE)	-C	./htslib
 	$(MAKE)	$(PROGRAMS)
 
-ALL_CPPFLAGS = -I. $(CPPFLAGS) -Ihtslib -Ijemalloc
-ALL_LDFLAGS  = $(LDFLAGS) -Lhtslib/htslib -Ljemalloc/lib
+ALL_CPPFLAGS = -I. $(CPPFLAGS) -I$(EBROOTHTSLIB)/include -I$(EBROOTJEMALLOC)/include
+ALL_LDDFLAGS = $(LDFLAGS) -L$(EBROOTHTSLIB)/lib -L$(EBROOTJEMALLOC)/lib -L$(EBROOTZLIB)/lib
 
 # Usually config.mk and config.h are generated by running configure
 # or config.status, but if those aren't used create defaults here.
@@ -37,7 +37,7 @@
 JEMLIB = $(JEMDIR)/lib/libjemalloc.a -ldl
 
 $(PROGRAMS): $(OBJ)
-	$(CXX) $(ALL_CPPFLAGS) $(ALL_LDFLAGS)	-o $@ $^ $(HTSLIB_LIB) $(JEMLIB)
+	$(CXX) $(ALL_CPPFLAGS) $(ALL_LDFLAGS)	-o $@ $^ -lhts -ljemalloc -lz
 
 %.o: %.cpp
 	$(CXX) $(ALL_CPPFLAGS) -o $@ -c $^
