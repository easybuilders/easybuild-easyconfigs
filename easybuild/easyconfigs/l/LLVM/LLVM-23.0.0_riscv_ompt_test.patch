# Author: julian.morillo@bsc.es
# Fix some OMPT tests that fail on RISC-V 
# (see https://github.com/llvm/llvm-project/pull/156914)
--- openmp/runtime/test/ompt/callback.h.ORIG	2026-07-24 10:25:59.882763073 +0200
+++ openmp/runtime/test/ompt/callback.h	2026-07-24 10:30:59.158744792 +0200
@@ -292,8 +292,8 @@
 // another branch).
 #define print_possible_return_addresses(addr)                                  \
   printf("%" PRIu64 ": current_address=%p or %p\n",                            \
-         ompt_get_thread_data()->value, ((char *)addr) - 6,                    \
-         ((char *)addr) - 10)
+         ompt_get_thread_data()->value, ((char *)addr) - 4,                    \
+         ((char *)addr) - 6, ((char *)addr) - 10)
 #else
 // On RV64G the NOP instruction is 4 byte long. In addition, the compiler
 // inserts a J instruction (targeting the successor basic block), which
