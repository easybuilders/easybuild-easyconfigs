# Author: julian.morillo@bsc.es
# The line '! UNSUPPORTED: riscv' ensures that this test is skipped when running on any RISC-V target (including riscv64)
# The 'RUN:' and 'CHECK' lines stay untouched, so the test will still run on architectures that support the required features
diff --git a/flang/test/Integration/debug-complex-1.f90 b/flang/test/Integration/debug-complex-1.f90
index 1234567..89abcde 100644
--- a/flang/test/Integration/debug-complex-1.f90
+++ b/flang/test/Integration/debug-complex-1.f90
@@
+! UNSUPPORTED: riscv

 ! REQUIRES: shell
 ! RUN: %flang_fc1 -emit-llvm -debug-info-kind=standalone %s -o - | FileCheck %s

