easyblock = 'Binary'

name = 'wigToBigWig'
version = '2.8'

homepage = 'http://hgdownload.cse.ucsc.edu/admin/exe/'
description = """Convert ascii format wig file (in fixedStep, variableStep or bedGraph format) 
 to binary big wig format.."""

toolchain = {'name': 'foss', 'version': '2021b'}
toolchainopts = {'pic': True}

source_urls = ['http://hgdownload.cse.ucsc.edu/admin/exe/linux.x86_64/']
sources = ['%(name)s']

sanity_check_paths = {
    'files': ['wigToBigWig'],
    'dirs': [],
}

sanity_check_commands = ['wigToBigWig 2>&1 | grep "wigToBigWig v %s"' % version]

moduleclass = 'lib'