# This file is an EasyBuild reciPY as per https://github.com/hpcugent/easybuild
# Authors: Pablo Escobar Lopez , Jordi Blasco
# Swiss Institute of Bioinformatics Biozentrum - University of Basel, HPCNow! Consulting SL - http://www.hpcnow.com

easyblock = 'MakeCp'

name = 'VCFtools'
version = '0.1.12b'

homepage = "http://vcftools.sourceforge.net/"
description = """The aim of VCFtools is to provide 
 methods for working with VCF files: validating, 
 merging, comparing and calculate some basic population
 genetic statistics. """

toolchain = {'name': 'foss', 'version': '2015a'}
toolchainopts = {'optarch': True}

source_urls = [SOURCEFORGE_SOURCE]
sources = ['%(namelower)s_%(version)s.tar.gz']

perl = 'Perl'
perlver = '5.20.1'
versionsuffix = '-%s-%s' % (perl, perlver)
dependencies = [
    (perl, perlver, '-bare'),
]

files_to_copy = ["bin", "lib", "examples", (["bin/man1"], 'man')]

modextrapaths = {'PERL5LIB': 'lib/perl5/site_perl'}

sanity_check_paths = {
    'files': ['bin/%s' % x for x in ['vcftools', 'vcf-sort', 'vcf-stats']],
    'dirs': [],
}

moduleclass = 'bio'
