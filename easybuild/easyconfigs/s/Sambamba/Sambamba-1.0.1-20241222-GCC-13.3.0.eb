easyblock = 'MakeCp'

name = 'Sambamba'
local_version = '1.0.1'
version = f'{local_version}-20241222'
local_commit = 'dba65af'

homepage = 'https://lomereiter.github.io/sambamba/'
description = """Sambamba is a high performance modern robust and fast tool
 (and library), written in the D programming language, for working with SAM
 and BAM files. Current functionality is an important subset of samtools
 functionality, including view, index, sort, markdup, and depth."""

toolchain = {'name': 'GCC', 'version': '13.3.0'}

source_urls = ['https://github.com/biod/sambamba/archive/']
sources = [{'download_filename': '%s.tar.gz' % local_commit, 'filename': '%(name)s-%(version)s.tar.gz'}]
checksums = ['e1ffc9d29e9cb1f76938a6e7086ad9df9639608f35b1ed97e99932df35268b1f']

builddependencies = [
    ('LDC', '1.42.0'),
    ('Python', '3.12.3'),
    ('lz4', '1.9.4'),
]

files_to_copy = [([f'bin/sambamba-{local_version}'], 'bin')]

postinstallcmds = [f"cd %(installdir)s/bin && ln -s sambamba-{local_version} sambamba"]

sanity_check_paths = {
    'files': ['bin/sambamba'],
    'dirs': [],
}

sanity_check_commands = [f"sambamba 2>&1 | grep '^sambamba {local_version}'"]

moduleclass = 'bio'
