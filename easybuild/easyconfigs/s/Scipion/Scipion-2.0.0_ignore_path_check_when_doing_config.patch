Don't check the path to the config file, we set that up correctly with the easyblock.

Åke Sandgren, 201009
diff -ru scipion-2.0.0.orig/pyworkflow/apps/pw_config.py scipion-2.0.0/pyworkflow/apps/pw_config.py
--- scipion-2.0.0.orig/pyworkflow/apps/pw_config.py	2019-05-07 10:40:13.000000000 +0200
+++ scipion-2.0.0/pyworkflow/apps/pw_config.py	2019-10-09 08:01:36.403433867 +0200
@@ -114,7 +114,7 @@
                           compare=options.compare)
 
         # After all, check some extra things are fine in scipion.conf
-        checkPaths(os.environ['SCIPION_CONFIG'])
+        #checkPaths(os.environ['SCIPION_CONFIG'])
         if (not globalIsLocal and '[BUILD]' in
             [x.strip() for x in open(os.environ['SCIPION_LOCAL_CONFIG'])]):
             print(red("Found a BUILD section in the local configuration file %s"
