Author: Julián Morillo, 2026-03-04
Reduce the number of resources used to avoid "Too many open files" error
--- scipy/_lib/tests/test_import_cycles.py.ORIG	2026-03-04 12:47:29.833850649 +0100
+++ scipy/_lib/tests/test_import_cycles.py	2026-03-04 12:48:59.329866894 +0100
@@ -20,7 +20,7 @@
 @pytest.mark.fail_slow(40)
 @pytest.mark.slow
 def test_public_modules_importable_2():
-    # Ensure we use max 6 processes, to limit peak resource usage (memory, file handles)
+    # Ensure we use max 2 processes, to limit peak resource usage (memory, file handles)
     # on resource-constrained systems (e.g., RISC-V - see gh-24163).
-    with multiprocessing.Pool(processes=6) as pool:
+    with multiprocessing.Pool(processes=2) as pool:
         pool.map(_check_single_module, PUBLIC_MODULES)
