For the EasyBuild use case we explicitly disable OpenMPI detection
(since this is just for testing, and we want Flux at GCCcore).

diff -rupN flux-pmix-0.6.0.orig/configure flux-pmix-0.6.0/configure
--- flux-pmix-0.6.0.orig/configure	2025-01-15 19:35:40.320811205 +0100
+++ flux-pmix-0.6.0/configure	2025-07-25 11:38:49.091578072 +0200
@@ -14579,7 +14579,9 @@ else
 	OMPI_LIBS=$pkg_cv_OMPI_LIBS
         { printf "%s\n" "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 printf "%s\n" "yes" >&6; }
-	have_ompi=yes
+	# For EasyBuild we don't want OpenMPI configured for the tests at all so hard set this from
+	# have_ompi=yes
+	have_ompi=no
 fi
 if test "${have_ompi}" = "yes"; then
   OMPI_PREFIX=`pkg-config --variable=prefix ompi`
diff -rupN flux-pmix-0.6.0.orig/t/sharness.d/00-setup.sh.in flux-pmix-0.6.0/t/sharness.d/00-setup.sh.in
--- flux-pmix-0.6.0.orig/t/sharness.d/00-setup.sh.in	2024-12-14 01:20:29.607561957 +0100
+++ flux-pmix-0.6.0/t/sharness.d/00-setup.sh.in	2025-07-25 11:31:27.141177971 +0200
@@ -1,2 +1 @@
-PATH=@OMPI_PREFIX@/bin:$PATH
 PATH=@FLUX_PREFIX@/bin:$PATH
