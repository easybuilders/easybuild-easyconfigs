--- amber18.orig/AmberTools/src/configure2	2021-03-05 00:35:18.161676041 +0000
+++ amber18/AmberTools/src/configure2	2021-03-05 00:37:11.824357036 +0000
@@ -2474,7 +2474,7 @@
 #  Support platforms without a C <complex.h> by building c9x-complex.
 #--------------------------------------------------------------------------
 
-if [ -r /usr/include/complex.h ]; then
+if [ -r $EBROOTGENTOO/include/complex.h ]; then
     c9xcomplex='skip'
 elif [ "$cygwin" = 'yes' -o "$windows" = 'yes' ]; then
     c9xcomplex='skip'
@@ -2489,7 +2489,7 @@
 #  Support platforms without <sys/dir.h> but with <dirent.h>
 #  usually a non-Linux Unix with a non-native compiler.
 #--------------------------------------------------------------------------
-if [ ! -r /usr/include/sys/dir.h  -a -r /usr/include/dirent.h ]; then
+if [ ! -r $EBROOTGENTOO/include/sys/dir.h  -a -r /usr/include/dirent.h ]; then
     if [ "$cygwin" != 'yes' ]; then
        cflags="$cflags -DSYSV"
        pmemd_coptflags="$pmemd_coptflags -DSYSV"
