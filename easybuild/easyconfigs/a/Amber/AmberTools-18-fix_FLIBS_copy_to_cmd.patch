FLIBS may be longer than 1024 chars and would then not fit into words.
Append it directly to cmd instead.

Åke Sandgren, 20190228
diff -ru amber18.orig/AmberTools/src/nab/nab.c amber18/AmberTools/src/nab/nab.c
--- amber18.orig/AmberTools/src/nab/nab.c	2018-04-15 23:45:11.000000000 +0200
+++ amber18/AmberTools/src/nab/nab.c	2019-02-28 13:14:23.999944738 +0100
@@ -231,13 +231,13 @@
 	}
 	if( !copt ){
 		sprintf( word, " -L%s/lib -lnab -lcifparse", amberhome );
-		if (strlen(cmd) + strlen(word) + strlen(FLIBS) + 7 > cmd_sz) {
-		    cmd_sz += strlen(word) + strlen(FLIBS) + 7;
+		if (strlen(cmd) + strlen(word) + strlen(FLIBS) + 6 > cmd_sz) {
+		    cmd_sz += strlen(word) + strlen(FLIBS) + 6;
 		    cmd = realloc(cmd, cmd_sz);
 		}
 		strcat( cmd, word );
-		sprintf( word, " %s ", FLIBS );
-		strcat( cmd, word );
+		strcat( cmd, " " );
+		strcat( cmd, FLIBS );
 		strcat( cmd, " -lm" );
 	}
 	if( cgdopt ) fprintf( stderr, "cc cmd: %s\n", cmd );
