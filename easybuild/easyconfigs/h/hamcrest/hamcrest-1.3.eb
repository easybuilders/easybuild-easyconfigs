easyblock = 'JAR'

name = 'hamcrest'
version = '1.3'

homepage = 'https://code.google.com/p/hamcrest'
description = "Library of matchers for building test expressions."

toolchain = {'name': 'dummy', 'version': 'dummy'}

source_urls = [GOOGLECODE_SOURCE]
sources = ['%(name)s-all-%(version)s.jar']

sanity_check_paths = {
    'files': ['%(name)s-all-%(version)s.jar'],
    'dirs': [],
}

moduleclass = 'tools'
