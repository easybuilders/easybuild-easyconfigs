# Author: sreenivasamurthy kolam <sreenivasamurthy.kolam@amd.com>

easyblock = 'ROCmComponent'

name = 'hipFFT'
version = '1.0.22'
versionsuffix = '-nompi'
_rocm_version = '7.2.3'

homepage = 'https://github.com/ROCm/hipFFT'
description = """hipFFT is an FFT marshalling library that supports rocFFT and cuFFT backends.

hipFFT exports an interface that doesn't require the client to change, regardless of the chosen
backend. It sits between your application and the backend FFT library, where it marshals inputs
to the backend and marshals results back to your application."""
docurls = ['https://rocm.docs.amd.com/projects/hipFFT/en/latest/']

toolchain = {'name': 'rocm-compilers', 'version': f'22.0.0-ROCm-{_rocm_version}'}

github_account = 'ROCm'
local_repo_name = 'rocm-libraries'
source_urls = [f'https://github.com/{github_account}/{local_repo_name}/archive']
sources = [{
    'download_filename': f'rocm-{_rocm_version}.tar.gz',
    'filename': f'{local_repo_name}-rocm-{_rocm_version}.tar.gz',
}]
checksums = ['300cc50720d40bad7c7ed1f6d67e8c5ebecaba62c07a6ea1cc5813c0ea2e41b5']

builddependencies = [
    ('CMake', '3.31.8'),
    ('rocm-cmake', '0.14.0'),
]

dependencies = [
    ('HIP', _rocm_version),
    ('hipRAND', '3.1.0'),
    ('rocFFT', '1.0.36', '-nompi'),
]

# Build from the projects/hipfft subdirectory
start_dir = 'projects/hipfft'

sanity_check_paths = {
    'files': ['include/hipfft/hipfft.h',
              f'lib/libhipfft.{SHLIB_EXT}',
              'lib/cmake/hipfft/hipfft-config.cmake'],
    'dirs': ['share/doc/hipfft',
             'include/hipfft'],
}

moduleclass = 'lib'
