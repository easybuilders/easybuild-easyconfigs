easyblock = 'ConfigureMake'

name = 'PostgreSQL'
version = '18.4'

homepage = 'https://www.postgresql.org/'
description = """PostgreSQL is a powerful, open source object-relational database system.
 It is fully ACID compliant, has full support for foreign keys,
 joins, views, triggers, and stored procedures (in multiple languages).
 It includes most SQL:2008 data types, including INTEGER,
 NUMERIC, BOOLEAN, CHAR, VARCHAR, DATE, INTERVAL, and TIMESTAMP.
 It also supports storage of binary large objects, including pictures,
 sounds, or video. It has native programming interfaces for C/C++, Java,
 .Net, Perl, Python, Ruby, Tcl, ODBC, among others, and exceptional documentation."""

toolchain = {'name': 'GCCcore', 'version': '15.2.0'}

source_urls = ['http://ftp.postgresql.org/pub/source/v%(version)s/']
sources = [SOURCELOWER_TAR_GZ]
checksums = ['450aa8f2da06c46f8221916e82ae06b04fb1040f8f00643dbf8b7d663caac0b9']

builddependencies = [
    ('binutils', '2.45'),
    ('Bison', '3.8.2'),
    ('flex', '2.6.4'),
    ('Perl', '5.42.0'),
    ('Python', '3.14.2'),
]

dependencies = [
    ('libreadline', '8.3'),
    ('zlib', '2.3.2'),
    ('OpenSSL', '3', '', SYSTEM),
]

configopts = '--with-python --with-openssl --without-icu'

sanity_check_paths = {
    'files': ['bin/psql', 'bin/pg_config', 'lib/libpq.a', f'lib/libpq.{SHLIB_EXT}'],
    'dirs': ['share/postgresql'],
}

moduleclass = 'data'
