# Use min/max functions from the STL library
# Author: maxim-masterov (SURF)
#
diff -Nru Gate-8.2.orig/source/general/include/GateIAEAUtilities.h Gate-8.2/source/general/include/GateIAEAUtilities.h
--- Gate-8.2.orig/source/general/include/GateIAEAUtilities.h	2022-01-03 10:55:29.448056000 +0100
+++ Gate-8.2/source/general/include/GateIAEAUtilities.h	2022-01-05 11:33:46.043596000 +0100
@@ -100,8 +100,8 @@
 /* #define max and min */
 #ifndef MINMAX_DEFINED
 #define MINMAX_DEFINED
-#define min(a,b)    (((a) < (b)) ? (a) : (b))
-#define max(a,b)    (((a) > (b)) ? (a) : (b))
+//#define min(a,b)    (((a) < (b)) ? (a) : (b))
+//#define max(a,b)    (((a) > (b)) ? (a) : (b))
 //#define __max       max
 //#define __min       min
 #endif /*MINMAX_DEFINED */
