easyblock = 'ConfigureMake'

name = 'GST-plugins-base'
version = '0.10.36'
pyver = '2.7.12'
pyshortver = '2.7'
pysubver = '-bare'
versionsuffix = '-Python-%s%s' % (pyver, pysubver)

homepage = 'http://gstreamer.freedesktop.org/'

description = """
 GStreamer is a library for constructing graphs of media-handling
 components. The applications it supports range from simple
 Ogg/Vorbis playback, audio/video streaming to complex audio
 (mixing) and video (non-linear editing) processing."""

toolchain = {'name': 'GCCcore', 'version': '6.3.0'}

source_urls = ['http://gstreamer.freedesktop.org/src/gst-plugins-base']
sources = [SOURCELOWER_TAR_XZ]

builddependencies = [
    ('Python', '2.7.12', '-bare'),
]

dependencies = [
    ('GStreamer', '0.10.36', versionsuffix),
]

osdependencies = [
    ('bison'),  # does not work with Bison 3.x
]

sanity_check_paths = {
    'dirs': ['include', 'share'],
    'files': ['bin/gst-%s-%%(version_major_minor)s' % x for x in
              ['discoverer', 'visualise']] +
             ['lib/libgst%s-%%(version_major_minor)s.%s' % (x, SHLIB_EXT) for x in
              ['app', 'audio', 'video']],
}

modextrapaths = {
    'GI_TYPELIB_PATH': 'share',
    'XDG_DATA_DIRS': 'share',
}

moduleclass = 'vis'
