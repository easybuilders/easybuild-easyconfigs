also consider path to binutils libraries directly in path specified to --with-binutils rather than in a lib* subdirectory,
because in a Gentoo Prefix installation libbfd.* and libiberty.* are located in a subdirectory like usr/lib64/binutils/x86_64-pc-linux-gnu/2.40
author: Kenneth Hoste (HPC-UGent)
--- extrae-4.0.6/configure.orig	2023-11-22 13:33:58.000000000 +0100
+++ extrae-4.0.6/configure	2024-03-20 10:10:36.899872909 +0100
@@ -34537,9 +34537,15 @@
          if test -r "${binutils_home_dir}/lib${BITS}/libbfd.so" -o \
                  -r "${binutils_home_dir}/lib${BITS}/libbfd.dylib" ; then
             BFD_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libbfd.so" -o \
+                 -r "${binutils_home_dir}//libbfd.dylib" ; then
+            BFD_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib${BITS}/libbfd.a" -a \
                     "${binutils_require_shared}" = "no" ; then
             BFD_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libbfd.a" -a \
+                    "${binutils_require_shared}" = "no" ; then
+            BFD_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib/${multiarch_triplet}/libbfd.so" -o \
                    -r "${binutils_home_dir}/lib/${multiarch_triplet}/libbfd.dylib" ; then
             BFD_LIBSDIR="${binutils_home_dir}/lib/${multiarch_triplet}"
@@ -34576,8 +34582,12 @@
 
          if test -r "${binutils_home_dir}/lib${BITS}/libiberty.so" -o -r "${binutils_home_dir}/lib${BITS}/libiberty.dylib" ; then
             LIBERTY_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libiberty.so" -o -r "${binutils_home_dir}/libiberty.dylib" ; then
+            LIBERTY_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib${BITS}/libiberty.a" ; then
             LIBERTY_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libiberty.a" ; then
+            LIBERTY_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib/${multiarch_triplet}/libiberty.a" ; then
             LIBERTY_LIBSDIR="${binutils_home_dir}/lib/${multiarch_triplet}"
          elif test -r "${binutils_home_dir}/lib/${multiarch_triplet}/libiberty.so" -o -r "${binutils_home_dir}/lib/${multiarch_triplet}/libiberty.dylib"; then
--- extrae-4.0.6/config/macros.m4.orig	2024-03-20 10:34:25.417043605 +0100
+++ extrae-4.0.6/config/macros.m4	2024-03-20 11:30:30.902224605 +0100
@@ -764,9 +764,15 @@
          if test -r "${binutils_home_dir}/lib${BITS}/libbfd.so" -o \
                  -r "${binutils_home_dir}/lib${BITS}/libbfd.dylib" ; then
             BFD_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libbfd.so" -o \
+                 -r "${binutils_home_dir}//libbfd.dylib" ; then
+            BFD_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib${BITS}/libbfd.a" -a \
                     "${binutils_require_shared}" = "no" ; then
             BFD_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libbfd.a" -a \
+                    "${binutils_require_shared}" = "no" ; then
+            BFD_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib/${multiarch_triplet}/libbfd.so" -o \
                    -r "${binutils_home_dir}/lib/${multiarch_triplet}/libbfd.dylib" ; then
             BFD_LIBSDIR="${binutils_home_dir}/lib/${multiarch_triplet}"
@@ -804,8 +810,12 @@
    
          if test -r "${binutils_home_dir}/lib${BITS}/libiberty.so" -o -r "${binutils_home_dir}/lib${BITS}/libiberty.dylib" ; then
             LIBERTY_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libiberty.so" -o -r "${binutils_home_dir}/libiberty.dylib" ; then
+            LIBERTY_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib${BITS}/libiberty.a" ; then
             LIBERTY_LIBSDIR="${binutils_home_dir}/lib${BITS}"
+         elif test -r "${binutils_home_dir}/libiberty.a" ; then
+            LIBERTY_LIBSDIR="${binutils_home_dir}"
          elif test -r "${binutils_home_dir}/lib/${multiarch_triplet}/libiberty.a" ; then
             LIBERTY_LIBSDIR="${binutils_home_dir}/lib/${multiarch_triplet}"
          elif test -r "${binutils_home_dir}/lib/${multiarch_triplet}/libiberty.so" -o -r "${binutils_home_dir}/lib/${multiarch_triplet}/libiberty.dylib"; then
